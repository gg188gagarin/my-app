import styled from 'styled-components';

export const Button = styled.button`
  align-items: center;
  gap: 0.75rem;
  line-height: 2.5;  box-shadow: var(--shadow);
  border-radius: var(--radii);
  color: var(--color-text);
  cursor: pointer;
  padding: 0 1rem;
  border: none;
  display: flex;
  background-color: var(--colors-ui-base);
`;
